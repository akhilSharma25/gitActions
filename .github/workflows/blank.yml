# This is a basic workflow to help you get started with Actions

name: Hello World
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "Hello , ${{secrets.DOCKER_PASSWORD
}}"

  

  
         


